---
applications:
  .:
    name: FollowMe
    framework:
      name: node
      info:
        mem: 64M
        description: Node.js Application
        exec: 
    infra: hp
    url: ${name}.${target-base}
    mem: '128'
    instances: 1
    services:
      Mongo:
        type: mongodb
